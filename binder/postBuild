#!/bin/bash
set -e

# Définir le français comme langue par défaut de JupyterLab
mkdir -p "${CONDA_PREFIX}/share/jupyter/lab/settings"
cat > "${CONDA_PREFIX}/share/jupyter/lab/settings/overrides.json" << 'EOF'
{
  "@jupyterlab/translation-extension:plugin": {
    "locale": "fr_FR"
  }
}
EOF
